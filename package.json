{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio website",
  "scripts": {
    "dev": "live-server --port=8080 --host=0.0.0.0 --no-browser",
    "start": "live-server --port=8080 --host=0.0.0.0 --no-browser",
    "dev-network": "live-server --port=8080 --host=0.0.0.0 --no-browser --entry-file=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
